<?php

declare(strict_types=1);

// Code generated by scripts/generate_rest_tests.py; DO NOT EDIT.
//
// AUTO-GENERATED full-mock REST wire tests for the 'datasphere' namespace — regenerate:
//   python3 scripts/generate_rest_tests.py
//
// Each route the SDK implements (captured from the real client by
// scripts/route_registry.php + scripts/rest_test_plan.php, joined to the spec
// operationId) gets a SUCCESS test (call it, assert method + matched_route on the
// mock journal) and an ERROR test (arm a 500, assert SignalWireRestError with
// getStatusCode() === 500). The assertion oracle is the spec operationId —
// independent of the resource generator — so these catch SDK-vs-contract drift,
// not a generator self-snapshot. Full-mock harness fixtures (MockTest).

namespace SignalWire\Tests\Rest\Generated;

use PHPUnit\Framework\Attributes\Test;
use PHPUnit\Framework\TestCase;
use SignalWire\REST\RestClient;
use SignalWire\REST\SignalWireRestError;
use SignalWire\Tests\Rest\Harness;
use SignalWire\Tests\Rest\MockTest;

class DatasphereGeneratedTest extends TestCase
{
    private RestClient $client;
    private Harness $mock;

    protected function setUp(): void
    {
        [$this->client, $this->mock] = MockTest::scopedClient();
    }

    #[Test]
    public function documentsCreateSuccess(): void
    {
        $this->client->datasphere()->documents()->create([]);
        $j = $this->mock->journal()->last();
        $this->assertSame('POST', $j->method);
        $this->assertSame('datasphere.create_document', $j->matchedRoute);
    }

    #[Test]
    public function documentsCreateError(): void
    {
        $this->mock->scenarios()->set('datasphere.create_document', 500, ['error' => 'x']);
        try {
            $this->client->datasphere()->documents()->create([]);
            $this->fail('expected SignalWireRestError');
        } catch (SignalWireRestError $e) {
            $this->assertSame(500, $e->getStatusCode());
        }
        $j = $this->mock->journal()->last();
        $this->assertSame(500, $j->responseStatus);
        $this->assertSame('datasphere.create_document', $j->matchedRoute);
    }

    #[Test]
    public function documentsDeleteChunkSuccess(): void
    {
        $this->client->datasphere()->documents()->deleteChunk('x', 'x');
        $j = $this->mock->journal()->last();
        $this->assertSame('DELETE', $j->method);
        $this->assertSame('datasphere.delete_document_chunk', $j->matchedRoute);
    }

    #[Test]
    public function documentsDeleteChunkError(): void
    {
        $this->mock->scenarios()->set('datasphere.delete_document_chunk', 500, ['error' => 'x']);
        try {
            $this->client->datasphere()->documents()->deleteChunk('x', 'x');
            $this->fail('expected SignalWireRestError');
        } catch (SignalWireRestError $e) {
            $this->assertSame(500, $e->getStatusCode());
        }
        $j = $this->mock->journal()->last();
        $this->assertSame(500, $j->responseStatus);
        $this->assertSame('datasphere.delete_document_chunk', $j->matchedRoute);
    }

    #[Test]
    public function documentsDeleteSuccess(): void
    {
        $this->client->datasphere()->documents()->delete('x');
        $j = $this->mock->journal()->last();
        $this->assertSame('DELETE', $j->method);
        $this->assertSame('datasphere.delete_document', $j->matchedRoute);
    }

    #[Test]
    public function documentsDeleteError(): void
    {
        $this->mock->scenarios()->set('datasphere.delete_document', 500, ['error' => 'x']);
        try {
            $this->client->datasphere()->documents()->delete('x');
            $this->fail('expected SignalWireRestError');
        } catch (SignalWireRestError $e) {
            $this->assertSame(500, $e->getStatusCode());
        }
        $j = $this->mock->journal()->last();
        $this->assertSame(500, $j->responseStatus);
        $this->assertSame('datasphere.delete_document', $j->matchedRoute);
    }

    #[Test]
    public function documentsGetChunkSuccess(): void
    {
        $this->client->datasphere()->documents()->getChunk('x', 'x');
        $j = $this->mock->journal()->last();
        $this->assertSame('GET', $j->method);
        $this->assertSame('datasphere.get_document_chunk', $j->matchedRoute);
    }

    #[Test]
    public function documentsGetChunkError(): void
    {
        $this->mock->scenarios()->set('datasphere.get_document_chunk', 500, ['error' => 'x']);
        try {
            $this->client->datasphere()->documents()->getChunk('x', 'x');
            $this->fail('expected SignalWireRestError');
        } catch (SignalWireRestError $e) {
            $this->assertSame(500, $e->getStatusCode());
        }
        $j = $this->mock->journal()->last();
        $this->assertSame(500, $j->responseStatus);
        $this->assertSame('datasphere.get_document_chunk', $j->matchedRoute);
    }

    #[Test]
    public function documentsGetSuccess(): void
    {
        $this->client->datasphere()->documents()->get('x');
        $j = $this->mock->journal()->last();
        $this->assertSame('GET', $j->method);
        $this->assertSame('datasphere.get_document', $j->matchedRoute);
    }

    #[Test]
    public function documentsGetError(): void
    {
        $this->mock->scenarios()->set('datasphere.get_document', 500, ['error' => 'x']);
        try {
            $this->client->datasphere()->documents()->get('x');
            $this->fail('expected SignalWireRestError');
        } catch (SignalWireRestError $e) {
            $this->assertSame(500, $e->getStatusCode());
        }
        $j = $this->mock->journal()->last();
        $this->assertSame(500, $j->responseStatus);
        $this->assertSame('datasphere.get_document', $j->matchedRoute);
    }

    #[Test]
    public function documentsListChunksSuccess(): void
    {
        $this->client->datasphere()->documents()->listChunks('x');
        $j = $this->mock->journal()->last();
        $this->assertSame('GET', $j->method);
        $this->assertSame('datasphere.list_document_chunks', $j->matchedRoute);
    }

    #[Test]
    public function documentsListChunksError(): void
    {
        $this->mock->scenarios()->set('datasphere.list_document_chunks', 500, ['error' => 'x']);
        try {
            $this->client->datasphere()->documents()->listChunks('x');
            $this->fail('expected SignalWireRestError');
        } catch (SignalWireRestError $e) {
            $this->assertSame(500, $e->getStatusCode());
        }
        $j = $this->mock->journal()->last();
        $this->assertSame(500, $j->responseStatus);
        $this->assertSame('datasphere.list_document_chunks', $j->matchedRoute);
    }

    #[Test]
    public function documentsListSuccess(): void
    {
        $this->client->datasphere()->documents()->list();
        $j = $this->mock->journal()->last();
        $this->assertSame('GET', $j->method);
        $this->assertSame('datasphere.list_documents', $j->matchedRoute);
    }

    #[Test]
    public function documentsListError(): void
    {
        $this->mock->scenarios()->set('datasphere.list_documents', 500, ['error' => 'x']);
        try {
            $this->client->datasphere()->documents()->list();
            $this->fail('expected SignalWireRestError');
        } catch (SignalWireRestError $e) {
            $this->assertSame(500, $e->getStatusCode());
        }
        $j = $this->mock->journal()->last();
        $this->assertSame(500, $j->responseStatus);
        $this->assertSame('datasphere.list_documents', $j->matchedRoute);
    }

    #[Test]
    public function documentsSearchSuccess(): void
    {
        $this->client->datasphere()->documents()->search('x');
        $j = $this->mock->journal()->last();
        $this->assertSame('POST', $j->method);
        $this->assertSame('datasphere.search_documents', $j->matchedRoute);
    }

    #[Test]
    public function documentsSearchError(): void
    {
        $this->mock->scenarios()->set('datasphere.search_documents', 500, ['error' => 'x']);
        try {
            $this->client->datasphere()->documents()->search('x');
            $this->fail('expected SignalWireRestError');
        } catch (SignalWireRestError $e) {
            $this->assertSame(500, $e->getStatusCode());
        }
        $j = $this->mock->journal()->last();
        $this->assertSame(500, $j->responseStatus);
        $this->assertSame('datasphere.search_documents', $j->matchedRoute);
    }

    #[Test]
    public function documentsUpdateSuccess(): void
    {
        $this->client->datasphere()->documents()->update('x', []);
        $j = $this->mock->journal()->last();
        $this->assertSame('PATCH', $j->method);
        $this->assertSame('datasphere.update_document', $j->matchedRoute);
    }

    #[Test]
    public function documentsUpdateError(): void
    {
        $this->mock->scenarios()->set('datasphere.update_document', 500, ['error' => 'x']);
        try {
            $this->client->datasphere()->documents()->update('x', []);
            $this->fail('expected SignalWireRestError');
        } catch (SignalWireRestError $e) {
            $this->assertSame(500, $e->getStatusCode());
        }
        $j = $this->mock->journal()->last();
        $this->assertSame(500, $j->responseStatus);
        $this->assertSame('datasphere.update_document', $j->matchedRoute);
    }
}
