/vendor/
# composer.lock IS committed for reproducible installs (pinned dependency versions
# across dev + CI). Do not re-ignore it.
.phpunit.result.cache
.phpunit.cache/
.php-cs-fixer.cache
*.phar
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
.env
__pycache__/

# Claude Code local settings (never commit)
.claude/

# sw-verify repo-local scratch
.sw-tmp/

# Track the shared Claude Code permission set (rest of .claude/ stays local)
!.claude/
.claude/*
!.claude/settings.json
