# meta-consistent allowlist (php)

No active exceptions.

The former `floor:php` entry was retired 2026-07-13: the PHP floor is stated
consistently as **8.2** across every source the gate reads — `composer.json`
(`php >=8.2`), `README.md` ("Requires PHP 8.2+"), and
`porting-sdk/PORT_PHILOSOPHY_PHP.md` (updated 8.1→8.2) — so there is nothing to
allowlist.
